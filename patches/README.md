Local patches can live here when they are compatible with the upstream project license and do not bypass paid feature gates.

The current n8n deployment uses upstream `n8nio/n8n:latest` and Authentik proxy protection.

